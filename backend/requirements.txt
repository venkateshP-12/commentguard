# CommentGuard — requirements
# Classical model (default, lightweight)
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
scikit-learn>=1.5.0
joblib>=1.4.2
numpy>=2.0.0
pydantic>=2.7.1
python-multipart>=0.0.9

# Transformer model (required when MODEL_TYPE=transformer in .env)
# Used for unitary/toxic-bert via Hugging Face pipeline
transformers>=4.40.0
torch>=2.2.0
python-dotenv>=1.0.0

# Dev / testing
pytest>=8.0.0
httpx>=0.27.0